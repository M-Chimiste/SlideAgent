"""Polished native (editable) PPTX renderer.

Reproduces the HTML/CSS design-system look with real python-pptx shapes and text
(editable in PowerPoint), parameterized by the design-language ``Theme``. Replaces
the image-based HTML renderer.
"""
